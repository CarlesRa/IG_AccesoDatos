<Window x:Class="DataGrid.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Empleados"
        mc:Ignorable="d"
        Title="MainWindow" Height="447" Width="520">

    <Grid Margin="0,0,291,0">
        <DataGrid x:Name="dataGrid" AlternatingRowBackground="LightBlue" AlternationCount="2" AutoGenerateColumns="False" Margin="0,0,-290,0" Height="374" VerticalAlignment="Top">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" />
                <DataGridTextColumn Header="Affiliation" Binding="{Binding Affiliation}"/>
                <DataGridCheckBoxColumn Header="ReElected" Binding="{Binding WasReElected}" />
                <!--<DataGridComboBoxColumn Header="Party" Binding="{Binding Affiliation}" ItemsSource="{Binding Source={StaticResource myEnum}}"/>-->


            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Nuevo empleado" HorizontalAlignment="Left" Margin="10,388,0,0" VerticalAlignment="Top" Width="100" Click="Button_Click" />
    </Grid>
</Window>